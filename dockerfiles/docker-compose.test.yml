version: '3'
services:
  app:
    build: ../
    environment:
      RAILS_ENV: 'test'
      MYSQL_USERNAME: 'root'
      MYSQL_PASSWORD: ''
    ports:
      - 3000:3000
    depends_on:
      - db
  db:
    image: mysql:5.6.40
    environment:
      MYSQL_ALLOW_EMPTY_PASSWORD: 'yes'

.user-show
  .container
    = render partial: "shared/notification"
    -# TODO パンくずリストの追加？
    .row
      = render "sidebar"
      .main-content.main-content__reading.col-sm-9
        .card
          = render 'reading', { reading_book: @reading_book }